# Claude Code ソースコード セキュリティ監査結果

**監査日**: 2026-03-31
**監査対象**: `source/` ディレクトリ（1,902 ファイル / 38,057 行 / 34MB）
**ソース**: instructkr/claude-code アーカイブ（.map ファイルから復元された TypeScript ソース）

## 検査結果

| # | 検査項目 | 結果 | 詳細 |
|---|---|---|---|
| 1 | 悪意あるコード・バックドア | **検出なし** | `eval()`, `Function()` 未使用。`child_process` は正当なツール実行のみ |
| 2 | プロンプトインジェクション | **検出なし** | 38 個の `prompt.ts` すべて正当。防御機構が実装済み |
| 3 | 情報窃取パターン | **検出なし** | credentials 送信・keylogger・clipboard 窃取なし |
| 4 | 難読化コード | **検出なし** | `_0x` パターンなし。base64 は JWT 解析等の正当用途 |
| 5 | サプライチェーン攻撃 | **検出なし** | `postinstall` / `preinstall` スクリプト 0 件 |
| 6 | 不審な外部 URL 通信 | **検出なし** | Anthropic API 等の正規エンドポイントのみ |

## 防御実装の確認

ソースコード内に以下のセキュリティ防御機構が実装されていることを確認した。

### プロンプトインジェクション検出

- プロンプトインジェクション検出機構が組み込まれており、悪意ある入力の検知・ブロックが行われる

### コマンドインジェクション防止

- **bashSecurity.ts**（2,592 行）
- シェルコマンド実行時のインジェクション防止ロジックを包括的に実装

### DNS/HTTP Exfiltration 防止

- `gh` コマンドのホスト検証
- 3 層チェック（DNS 解決・HTTP リクエスト・レスポンス検証）によるデータ流出防止

### パストラバーサル防止

- `PathTraversalError` による検出・ブロック
- ファイルアクセス時のパス正規化と境界チェック

## 総合判定

**安全**

悪意あるコード、バックドア、情報窃取パターンは検出されなかった。ソースコードは正当な CLI ツールとして設計・実装されており、ユーザーのシステムに対する不正な操作は含まれていない。

防御機構については、プロンプトインジェクション・コマンドインジェクション・データ流出・パストラバーサルの各攻撃ベクトルに対して適切な対策が実装されていることを確認した。
